# Git Workflow

- Never commit to main
- Always create feature branches within git WORKTREE to avoid conflicts with other paralel work
- Use the ticket number as the branch name when available
- Always run code formatting and linting before committing
- Always run tests before committing
- Never append co-authoring by Claude information to the commit messages, pull requests descriptions or any input interaction
